import 'package:flutter/material.dart';

class UserAddsScreen extends StatelessWidget {
  const UserAddsScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('User Adds Screen'));
  }
}
